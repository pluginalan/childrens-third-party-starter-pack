require("amd-loader");

describe("packages",function () {

    var packages = require("../../src/demo-web-app/pnm-container/pnm/js/gmi-extensions/packages")

    beforeEach(function () {

    });
    it("Should cancel a download", function () {
        //expect(packages).toBe(packages)
    });
});